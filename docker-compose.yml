services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env          # здесь лежит BOT_TOKEN=...
    # логи не раздуваем
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
